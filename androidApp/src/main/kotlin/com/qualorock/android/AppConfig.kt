package com.qualorock.android

/** Points at the API exposed by the root `docker-compose.yml` `api` service (`QOR_API_PORT`, default 8080). */
object AppConfig {
    const val API_BASE_URL: String = "http://10.0.2.2:8080"
}
